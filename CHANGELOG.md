# 更新紀錄

## 1.26.0925 build 0134（2026-09-25）

- EV 加入高光柔和壓縮與暗部保護，適用於印相／觀看曝光及數位曝光；維持 RGB 比例、0 EV 外觀與 RAW 浮點餘裕，預覽與匯出共用。

- 原生影像處理改成逐階段完成並釋放暫存資源，重用兩個 FP32 工作緩衝區，降低完整解析度匯出的記憶體峰值。
- 略過沒有任何調整的明暗分區；乳劑捕獲場只計算一次，膚色統計與遮罩不再保留整條上游運算圖。
- 顯影動畫沿用目前照片的 2048 px 預覽快取，依處理進度顯現，完成時不再解碼匯出大檔。
- 保留 RAW 立即解碼與完整精度，避免延後解碼在後續渲染產生局部異色。

- 顯影動畫一般完成後約 1.2 秒收尾。
- 選取照片時保留縮圖列位置，不再自動左右移動。

## 1.26.0924 build 2244（2026-09-24）

自訂底片分享與照片瀏覽更新。已合併 1.26.0924 build 2228 的全部更新內容。

- 自訂底片卡片新增「匯出」，可將配方儲存成 JSON 檔案；底片收藏標題右側新增「匯入」。
- 匯入時，相同 ID 會覆蓋既有配方，不同 ID 則新增；同名但不同 ID 會自動加上編號。匯入不會自動改變目前照片。
- 每張縮圖的檔名右側顯示 `#0001` 格式的序號，目錄標題下方也會顯示目前第幾張與總張數，方便定位照片。
- 縮圖列的水平捲軸保持可見，方便拖動瀏覽；直向捲軸仍會在停止操作後隱藏。
- 選取照片目錄後自動開啟第一張照片；重新啟動時仍保留上次編輯的照片。
- 儲存正在使用的自訂底片時，自動帶入原名稱。保留原名會更新這款底片，改成新名稱則另存一款；不會覆寫其他同名底片。

適用 Apple Silicon Mac、macOS 14 以上。DMG 使用 Developer ID 簽章並完成 Apple 公證。

X100V 色彩研究仍為本地測試，本版未收錄。

## 1.26.0924 build 1826（2026-09-24）

- 新增「模擬相機」：GR III 負片／高反差黑白、GR IV Cinema Yellow／Green；預設強度 50。依五場景比較排除與 Velvia 50 相近的 GR III 正片，模擬為獨立近似，並非原廠 LUT。
- 底片精簡為 18 款：Portra 160／800 → 400、VISION 250D → 50D、200T → 500T、FP4 → HP5。收藏與排序對應代表款，既有照片及自訂底片仍保留原配方。
- 修正中性掃描略過各底片的印相反差、最大密度與保留銀特徵，改善款式區隔；保留 FP32 精度。
- 底片庫改為緊湊五欄卡片，依類別分組、組內依名稱排序，類別間有分隔線；移除套用按鈕與目前底片大卡片，AI 描述與縮小的名稱並排。請從工作台選用已勾選的款式。
- 修正繁體中文排序語系造成底片庫無法開啟、GR 系列 Hover 預覽未被接受，以及程式還原捲動位置時喚醒捲軸。捲軸停止操作約一秒後隱藏。

驗證：共用底片核心測試 60 項，59 項通過、1 項效能測試依設定略過；四款 GR 共 60 張正式流程渲染、參數儲存還原、黑白中性與強度端點、四語系分類與選取、原生 WebKit Hover 回傳及快取均通過。既有照片 ID 和配方保留；掃描核心修正會影響重新渲染的底片外觀。

## 1.26.0924 build 1508（2026-09-24）

- 完成兩輪檢查：修正 HDR 重複濾波實作、奇數／狹長影像取樣，以及 HDR、局部明暗、膚色與背景模糊的透明度處理。
- 防止重新載入或重設修復後，舊的非同步人物遮罩覆蓋新照片；匯出重新辨識失敗時保留既有有效遮罩。
- AI 分析與主體辨識改用已套用修復筆刷的影像；結束程式前等待已接受的預覽與匯出工作完成。
- 修正預覽解碼失敗後的重試：重新傳送圖片，且相同資料 URL 也會重新解碼。操作、介面與預設值維持不變。
- 膚色及鏡頭模糊的人物遮罩從高斯柔化升級為完整 RGB 聯合導向濾波，減少跨色邊界滲漏與人物外圍的清晰背景圈。
- 採用 Fast Guided Filter 的低解析度係數估計、原解析度重建，並重用共用中間結果；不需新增模型下載。
- 統一方框濾波的半徑／視窗寬度換算，修正部分預覽尺寸磨皮中段強度無效及 HDR 基底／細節分離範圍偏小。
- 快取預覽來源圖片，僅向介面傳送變動圖像；切圖、裁切、修復及重新載入會正確失效或重新同步。
- 顆粒／紅暈及曝光處理共用中間結果，保留 FP32 與原有取樣密度。
- 補驗 Canon EOS 5D Mark IV CR2、EOS R CR3、Sony α7 III 壓縮／未壓縮 ARW 的完整解碼、預覽及匯出（macOS 27.0）。

## 1.26.0924 build 1137（2026-09-24）

- 修復筆刷：塗抹後放開滑鼠自動移除物件、補齊背景，支援復原／重做、照片各自儲存及完整解析度匯出。
- 首次使用修復模型時顯示可取消的下載進度對話框；完成下載後可離線處理，照片不會上傳。
- 新增膚色冷暖滑桿，範圍 −100～100，預設 0，僅在膚色遮罩內作用。
- 磨皮改用保留邊緣的導向濾波；調整去雜訊順序，避免抵消底片顆粒，並改善美白強度與暗角補償。
- 改善手動 HDR、裁切後深度對齊與模擬鏡頭模糊的主體邊緣滲色。
- 修正掃描／正片模式印相光源無法調整，改用成品光源色彩補償；原始參考保留既有外觀。
- 恢復預設值會立即清除已套用的修復筆刷結果。
- 筆刷工具列對齊照片區域、內容靠右；匯出按鈕改成陶紅底、白色文字與圖示。
- 更新四語 README、共用影像處理說明與第三方模型授權資訊。

## 系統需求

Apple Silicon、macOS 14 以上。修復筆刷首次使用需下載約 217 MB 的 LaMa 模型，後續可離線使用。大範圍修復可能產生不自然紋理，請放大檢查。

## 前版驗證（build 1508）

共用影像處理測試 266 項，265 項通過、1 項效能測試依設定略過；另外 3 項 Swift Testing 通過。涵蓋獨立 Double 精度數學參考、色彩邊界、快速取樣、透明度、裁切座標與磨皮中段強度。另通過正式版建置、原生非同步回歸、影像精度、修復筆刷、儲存還原、模型目錄／下載、MCP、底片參數與像素 Smoke 測試。

本版另通過 28 項 WebKit 導航檢查、31 項影像精度檢查、1,272 項底片／AI 參數契約斷言與 123 項底片像素斷言。桌面 Smoke 涵蓋兩種預覽解碼失敗重試、載入動畫、取消匯出及既有檔案保護。

驗證環境為 Apple Silicon、macOS 27.0。完整歷史 UI 劇本仍有過時選擇器，未列為全套通過；大型語言模型推論本次未全面重跑，AI 輸入以像素可檢查的替身驗證，真實 LaMa 修復模型另有 Smoke 測試。
